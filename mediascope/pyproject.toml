[project]
name = "mediascope-starter"
version = "0.1.0"
description = "Video search query classification service"
requires-python = ">=3.11"
dependencies = [
    "pandas>=2.0",
    "numpy>=1.24",
    "scikit-learn>=1.3",
    "requests>=2.31",
    "python-dotenv>=1.0",
]

[tool.uv]
package = false
